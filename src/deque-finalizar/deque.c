#include <stdio.h>
#include <stdlib.h>
/*

typedef struct Node
{
    int data;
    struct Node *next;
    struct Node *prev;
} Node;

typedef struct List
{
    Node *start;
    Node *end;
    int size;
} Deque;

void pause()
{
    printf("\nPressione ENTER para continuar...");

    while (getchar() != '\n')
        ;

    getchar();
}

int main()
{
    int comando;

    while (1)
    {
        printf("\n----------- Menu -----------\n");
        printf("[0] Encerrar o programa\n");

        scanf("%d", &comando);

        switch (comando)
        {
        case 0:
            return 0;

        default:
            printf("Comando invalido!\n");
        }
    }
}

*/